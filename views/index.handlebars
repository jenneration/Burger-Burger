
    

  <div class="container"> 
    <h1 class="header text-warning">BurgerEats</h1>
      <img src="../assets/img/Hand-drawn-burger-on-transparent-background-PNG.png" class="burger-img" alt="hand drawn burger" width=275px;>
s
    </div>
        <div class="row justify-content-md-center">
          <div class="col-4">
            <h2 class="h2s">New Burgers</h2>
              <ul>
        {{#each burger}}
        {{#unless devoured}}
        {{> burgers/burger-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-4">
      <h2 class="h2s">Chewed Burgers</h2>
      <ul>
        {{#each burger}}
        {{#if devoured}}
        {{> burgers/burger-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>

    <form class="create-form" id="create-burger">
    <h2 class="h2s">Create Burger?</h2>
      <label for="bg"></label>
      <input class="area-jg" type="text" id="bg" name="name">
  <button class="magic" type="submit">Magic Burger-Maker Button</button>
  </form>
  </div>
</div>
</div>
